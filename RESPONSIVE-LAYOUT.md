# 桌面组件响应式布局方案（J.A.R.V.I.S. 赛博朋克可视化器）

> 适用范围：`src/App.jsx` 渲染的全部浮于 canvas 之上的 HUD/面板组件。
> 配套实现：`src/styles/cyber.css`（响应式布局层）、`src/hooks/useChatController.js`、`src/App.jsx`。
> 设计前提：大屏（≥1100px）保持现有 `position: fixed` 角定位的 1:1 视觉还原；中/小屏做协调重排，杜绝重叠。

---

## 1. 现状诊断：为什么会“不一致 / 重叠”

根因：**几乎所有 HUD 组件都用 `position: fixed` + 硬编码像素偏移**，且只有零散的 `@media (max-width: 640px)` 规则，缺少系统性的断点协调。

| 组件 | 选择器 | 定位（原值） | z-index | 现有响应式？ |
|---|---|---|---|---|
| 标题 | `.title-wrap` | fixed top:14 居中 | 7 | ❌（仅字号 clamp） |
| 多 Key 切换 | `.key-switch` | fixed top:14 right:16 | 9 | ❌ |
| 形态切换 | `.form-switch` | fixed top:64 居中 | 9 | ❌（仅 dev 可见） |
| 左下 HUD | `.hud.bl` | fixed bottom:18 left:16 | 7 | ❌ |
| 底部任务栏 | `.task-bar` | fixed bottom:16 居中 | 9 | ❌ |
| 油价卡 | `.oil-dock` | fixed top:16 left:16 | 8 | ⚠️ 旧规则把它塞到 `bottom:54` |
| MCP 浮层 | `.mcp-panel` | fixed top:80 left:16 (宽 300) | 60 | ⚠️ 仅 `max-width` 夹取 |
| 对话窗 | `.chat-panel` | fixed 居中，宽 `min(620,94vw)` | 11 | ⚠️ `.static` 移动端降级 |
| 追踪浮层 ×6 | `.float-panel` | fixed，可拖拽 | 60 | ❌ |
| 调试清单 | `.dev-legend` | fixed left:140 top:444 | 33 | ❌ |
| 黑客流区 | `.hacker-drag-zone` | fixed，JS `{x:296,y:88}` | 8 | ⚠️ 移动端隐藏 |

### 两个被实证的“放大器”Bug
1. **对话窗默认几何硬编码 `x: 1280`**（`useChatController.defaultRect()`）。任何窄于 ~1790px 的窗口都会把它 `clampRect` 到屏幕最右缘，与“居中”设计意图相悖 → 视觉不一致。
2. **MCP 浮层默认开启**（`App` 中 `mcpOpen` 初始 `true`），作为左上侧栏在 **≤1260px** 时与居中对话窗左缘直接重叠；叠加旧 `.oil-dock { bottom: 54px }` 规则，小屏底部带（油价卡 / 任务栏 / 对话窗底）相互打架。

---

## 2. 响应式布局总体方案

把“一堆互相独立的 fixed 岛屿”改造成 **四档屏宽协调的 zones（区域）模型**：

- **顶部控制带（Top Band）**：标题（中）、Key 切换（右上）、油价卡（左上）三处固定角位 + 尺寸收敛，互不侵占。
- **底部控制带（Bottom Band）**：任务栏（居中，唯一主交互条） + 左下 HUD（角落状态徽标），分车道。
- **主内容区（Center Stage）**：对话窗 + 追踪浮层，居中并 clamp 进视口。
- **按需浮层（On-demand Overlays）**：MCP / 调试清单 —— 默认收起，展开时以“底部抽屉 / 角落卡片”形态出现，与中心区**互斥**而非压盖。

大屏沿用原样；中/小屏通过新增的 **响应式布局层**（纯 `@media` 覆盖，桌面规则零改动）重排。

---

## 3. 断点系统与屏幕区间定义

| 区间 | 屏宽 | 代号 | 布局策略 |
|---|---|---|---|
| **大屏** | ≥ 1100px | XL | 原始 fixed 角定位，1:1 还原（本方案不干预） |
| **中屏** | 901 – 1099px | LG | 顶部带收敛；MCP 改底部抽屉；对话窗保持居中 |
| **小屏** | 641 – 900px | MD | 控件缩放、紧凑化；隐藏次要标签 |
| **超小屏** | ≤ 640px | SM | 触发 `.static` 移动端降级；底部只留任务栏 |
| （细分） | ≤ 420px | — | 再收敛字号与间距 |

> 阈值与 `useChatController.isTouchDevice()` 的 `innerWidth <= 640` 对齐，保证 JS 降级与 CSS 断点一致。

---

## 4. 组件在各断点下的排列与缩放规则

| 组件 | 大屏 (≥1100) | 中屏 (≤1099) | 小屏 (≤900) | 超小屏 (≤640) |
|---|---|---|---|---|
| 标题 | top:14 居中 | top:12；字距 4px | 同中屏 | 同中屏；≤420 字号 `clamp(16,6vw,22)` |
| Key 切换 | 右上，含“KEY”标签 | 同大屏 | **隐藏标签**，按钮 `12px` | 再紧凑 `11px` |
| 油价卡 | 左上 top:16 | 左上 + `max-width:42vw` | 左上 `max-width:46vw` | 左上 `max-width:44vw`（**撤销旧 bottom:54**） |
| 形态切换 | 居中 top:64（dev） | 同大屏 | top:56，按钮 `14px` | 同小屏 |
| 左下 HUD | bottom:18 left:16 | 同大屏 | 同大屏 | **bottom:60、z-index:12 浮于对话窗之上**，紧凑 `10px` |
| 任务栏 | 底部居中 | 同大屏 | 同大屏 | 隐藏“TASK”标签，再紧凑 |
| 对话窗 | 居中，宽 `min(620,94vw)`，高 `calc(100vh-154)` | 同大屏 | 同大屏 | `.static`：top:96 / bottom:56 / 宽 `94vw` |
| MCP 浮层 | 左上侧栏 top:80（默认关闭） | **底部抽屉** bottom:60，限高 `44vh` | 同中屏 | 底部抽屉 bottom:56，限高 `46vh` |
| 追踪浮层 ×6 | 可拖拽 | 可拖拽 | 可拖拽 | `max-width: min(360px, 92vw)` clamp |
| 调试清单 | left:140 top:444 | 同大屏 | 同大屏 | 移右下 `right:8 top:60`，限宽 |
| 黑客流区 | 默认 `{x:296,y:88}` | 同大屏 | 同大屏 | 隐藏 |

**缩放规则**：字号、内边距、间距全部随断点阶梯收敛（12→11→10px），而不是连续漂移；几何尺寸用 `min()/max()` + `vw` 做**有界流式**（fluid-with-clamp），避免极端窄屏下塌缩或溢出。

---

## 5. 避免重叠的约束条件

### 5.1 最小间距（硬约束）
- 定义 `--gap-min: 12px` 作为任意两块面板之间的最小净空；所有角位组件外缘距视口边缘统一 `12px`（原为 16px，小屏收紧）。
- 顶部带三元素（标题/Key/油价）**固定到三个互不重叠的角区**：标题占中央文本区（不占满宽，仅 inline-block），Key 占右上、油价占左上，横向天然分离。
- 底部带分车道：**任务栏 = 水平居中（唯一）**，**HUD = 左下角**，**对话窗底 = 居中之上**，三者纵向/横向错开。

### 5.2 换行策略（Wrap）
- 顶部带不做横向换行（避免标题断裂），改用 **尺寸收敛 + 标签隐藏**（小屏隐藏 Key 的“KEY”标签、任务栏的“TASK”标签）腾出空间。
- 任务栏内部按钮组 `display:flex; gap` 本就可在超窄屏自然压缩；若未来按钮更多，可加 `flex-wrap: wrap` 并限制最大行数。
- 浮层（追踪 ×6）**不自动换行堆叠**（避免遮挡），统一 `max-width: 92vw` 夹取，超出视口的 spawn 由 JS 夹取到视口内（见 §7）。

### 5.3 层级阶梯（z-index ladder，避免视觉打架）
```
背景层 0–6 ｜ HUD 7 ｜ 控制带 8–9 ｜ 对话窗 11 ｜ HUD(移动端浮标) 12 ｜ 浮层/调试 32–60
```
- 移动端 HUD 抬到 `z-index:12` 仅为了在小屏“浮于对话窗之上”作状态徽标；其 `pointer-events:none` 不阻断对话操作。
- MCP / 追踪浮层（60）始终高于对话窗（11），作为“主动展开的独立层”，而非悄悄压住内容。

---

## 6. 桌面 → 移动的流式 / 弹性布局策略

1. **有界流式尺寸（Fluid-with-Clamp）**：宽度用 `min(px, vw%)`、字号用 `clamp()`，让组件随视口平滑收缩但**有上下界**，杜绝塌缩/溢出跳变。
2. **断点阶梯收敛（Step Scaling）**：在 1099 / 900 / 640 / 420 四个档位做离散收敛（间距、字号、是否隐藏标签），兼顾“平滑感知”与“可控的一致性”。
3. **弹性容器思维**：顶部/底部控制带虽由独立 fixed 元素构成（受既有 JSX 结构约束，未引入外层 wrapper 以免破坏 1:1 还原），但通过**角位固定 + 尺寸收敛**模拟了 flex 的“两端对齐 + 居中”效果；未来若要彻底弹性化，可在 `App.jsx` 外包一层 `<div class="top-band">` / `<div class="bottom-band">` 用 `display:flex; justify-content:space-between` 承载，即可彻底消除坐标硬编码。
4. **降级而非堆叠**：小屏把“多窗口并存”降级为“单主窗 + 按需抽屉”，符合移动端单手操作与有限面积的物理约束。

---

## 7. 各屏幕区间的排布逻辑与冲突处理机制

### 大屏（≥1100）—— 并存模式
- 逻辑：所有面板按角位并存，中心留给对话窗。**冲突处理**：MCP 左上侧栏与对话窗左缘的净空由“对话窗居中 + 侧栏限宽 300”保证（≥1260px 时不重叠）。

### 中屏（901–1099）—— 抽屉化
- 逻辑：顶部带收敛；**MCP 由“左上侧栏”切换为“底部抽屉”**（bottom:60，限高 44vh），从根上消除与居中对话窗的横向重叠。
- 冲突处理：MCP 展开时作为底部独立层（z60）覆盖对话窗下缘，属用户主动行为；且**默认关闭**，常规浏览零重叠。

### 小屏（641–900）—— 紧凑化
- 逻辑：隐藏 Key/任务栏的次要标签，按钮与字号阶梯缩小，油价卡限宽 `46vw`。
- 冲突处理：顶部三元素仅靠尺寸收敛即不打架；对话窗保持居中（`min(620,94vw)`），不与任何角位组件重叠。

### 超小屏（≤640）—— 单主窗降级
- 逻辑：对话窗进入 `.static`（JS 加类）：居中、宽 `94vw`、上下留白（top:96 / bottom:56）。底部只留居中的任务栏；油价卡回到左上、Key 回到右上、标题回到中上。
- 冲突处理：
  - **撤销旧 `oil-dock{bottom:54}`**，油价卡回左上，不再与任务栏/对话窗底打架；
  - **左下 HUD 抬到对话窗之上（z12）作角落浮标**，避免被居中对话窗完全遮挡；
  - MCP 展开即底部抽屉（bottom:56，限高 46vh），与任务栏（bottom:14）上下错开；
  - 调试清单移到右下角限宽，避让顶部 Key 切换。

### 持久化坐标的回退机制（关键）
- 对话窗 / 可拖拽 HUD 的位置存于 `localStorage`。**`defaultRect()` 改为视口相对（居中 + 按视口收敛）**，新窗口/新设备首次加载即正确居中，不再被旧 `x:1280` 带偏。
- `clampRect()` 在 `resize` 时把几何重新夹回视口内，防止越界/半屏外。
- 建议（可选增强）：恢复存档时若保存视口与当前视口差异较大（如跨档），自动回退到 `defaultRect()` 重新居中，避免“在大屏拖好的布局在小屏仍右偏”。

---

## 8. 已落地的具体改动（实施清单）

| 文件 | 改动 |
|---|---|
| `src/styles/cyber.css` | 新增“响应式布局层”：四档断点（1099/900/640/420）+ 顶部带收敛 + MCP 底部抽屉 + 油价卡修正 + HUD 移动端浮标 + 浮层 clamp + 调试清单重定位 |
| `src/hooks/useChatController.js` | `defaultRect()` 由硬编码 `x:1280` 改为视口相对居中、有界收敛 |
| `src/App.jsx` | `mcpOpen` 初始 `true → false`，消除默认开启的 MCP 侧栏在 ≤1260px 与对话窗的重叠 |

> 桌面（≥1100px）视觉与交互未做任何改动，保证 1:1 还原。

---

## 9. 验证方法

1. `npm run dev` 启动，用浏览器 DevTools 设备工具栏依次切到 **1280 / 1024 / 768 / 390(iPhone)** 四档宽度：
   - 大屏：对话窗居中、MCP 入口在任务栏，点开为左上侧栏；
   - 中屏：MCP 点开为底部抽屉，不与对话窗重叠；
   - 超小屏：对话窗 `.static` 居中铺满中部，顶部三角（标题/Key/油价）+ 底部任务栏清晰分离，无重叠。
2. 拖拽对话窗后在更小宽度下刷新，确认其被 `clampRect` 夹回视口内、不再越界。
3. 检查 console 无布局相关报错；确认大屏视觉与改动前一致（可 `git diff` 核对仅新增媒体查询块）。
