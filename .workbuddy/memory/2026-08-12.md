# 2026-08-12 工作日志

## 音频频谱可视化器（赛博朋克风格）
- 创建单文件 HTML 实时频谱可视化器：72 条霓虹频谱柱 + 中央环形波形，模拟数据驱动（Math.sin + 噪声），深色主题响应式。
- 赛博朋克风格重构：霓虹光效（青/品红/紫）、透视网格、扫描线、故障艺术（glitch）、数字雨、HUD 面板、四角边框。
- 工程化重构：CONFIG 配置中心、state 对象化、语义化命名、边界条件处理、DOM 空值保护、dt clamp、visibilitychange 防跳变。
- 移除播放/暂停按钮与四角箭头元素：清除 CSS/HTML/JS 全部相关代码（toggleBtn/setRunning/事件监听），调整底部 HUD 位置，动画循环简化为持续运行。

## AI 聊天界面
- 新建 `ai-chat.html`：单文件零依赖响应式 AI 聊天 UI。
- 功能：消息分栏（AI 左 / 用户右）、输入框自适应高度、Enter 发送、 typing 加载态、localStorage 对话持久化、清空按钮。
- AI 回复当前为本地模拟（buildSimulatedReply + 模拟延迟），预留 callLLM() 注释模板可接入 OpenRouter 等真实 API。
- 健壮性：localStorage 容错、消息长度上限、busy 防重复发送、textContent 防 XSS、isComposing 处理中文输入法。

## 在可视化器页面集成 AI 助手（非新建页面）
- 在 `audio-visualizer.html` 内集成 AI 聊天助手，复用页面既有 CSS 变量（--cyan/--magenta/--dim）与霓虹/HUD/毛玻璃设计语言，未引入新配色字体组件。
- 右上角 HUD 面板新增「▸ AI 对话」入口，右侧滑入式抽屉面板（chat-panel），AI 左/用户右分栏、打字指示器、localStorage 持久化。
- 回复为本地模拟（buildReply + 延迟），预留 getAIResponse() 注释模板可接真实 API。
- 校验：页面两块 <script> 语法均通过。

## 频谱引擎抽取 + 自动化测试（集成与质量保障）
- 新增 `spectrum-engine.js`（UMD：浏览器挂 window.SpectrumEngine，Node 可 require），承载频谱模拟、快照、AI 实时数据回复逻辑；可视化器与 AI 助手共用同一份实现与实时数据（window.SpectrumAPI.snapshot 桥接）。
- 可视化器脚本改为复用引擎：CONFIG/状态/缓冲来自引擎，simulateSpectrum 委托引擎，dt 非有限值按 0、并限制 [0,0.05]，dt<=0 直接 no-op。
- 聊天回复改为 ENGINE.buildSpectrumReply(snap, text)，可读取实时能量/频段作答（如“当前能量多少”）。
- 新增 `tests/spectrum.test.js`（node --test，28 用例）：纯函数单元、模拟推进、快照、AI 回复、边界（dt=0/负/极大/NaN/长时运行）、集成（可视化↔AI 共享数据）、DOM 冒烟（元素引用/脚本解析/引擎接入）。全部通过（28/28）。
- 注意：页面现依赖同目录 spectrum-engine.js（非严格单文件）。

## AI 对话主窗口（无侧边栏布局）
- 新建 `ai-chat-main.html`：全屏单栏布局，对话列绝对居中、贯穿视口，无任何侧边栏/辅助栏；顶部条为主窗口标题栏、输入区常驻底部。
- 桌面端对话列 max-width 880px 居中、两侧极淡分线强调主窗口边界；移动端（≤600px）全宽、收起边线。
- 新增空状态居中引导（emptyState），无消息时显示「开始你的对话」；发送后隐藏、清空后恢复。
- 复用既有模拟回复逻辑与健壮性设计（localStorage 容错、maxChars、busy 防重复、textContent 防 XSS、isComposing、自动高度、Enter/Shift+Enter）。
- 校验：内联脚本 new Function 通过；所有 getElementById 引用的 6 个 id 均在 DOM 中定义（无报错隐患）。

## audio-visualizer.html：AI 对话窗口重构为「主对话窗口」（非侧栏）
- 将右侧滑入抽屉（.chat-panel right drawer）重构为居中前景主窗口：top:58px / bottom:96px / 水平居中，宽 min(620px,94vw)，z-index 11（最高优先级，高于 HUD/扫描线/暗角）。
- 视觉优先级：毛玻璃(blur 9px)+强霓虹描边与辉光，作为前景主窗口；音频可视化退为环绕背景氛围层（透过毛玻璃可见，互不遮挡），四角 HUD 仍可见。
- 功能/布局明确：页面核心交互区、主要沟通入口；默认开启（setChat(true)，不自动聚焦避免移动端弹键盘），右上角 HUD 入口改为 toggle（开启显示「▾ 收起对话」/ active 变品红），可一键收起回到纯可视化。
- 覆盖底部 HUD 冲突：chat bottom 设为 96px，避开 bottom:18px 的四角 HUD 面板。
- 校验：两段 <script> 语法 OK；12 个 getElementById 引用 id 全部在 DOM 定义；无 openChat/closeChat 残留调用。

## 可拖拽/可缩放半透明对话框组件（DialogController）
- CSS 重构 .chat-panel 定位：从 top/bottom 锚定改为 left/top/width/height 显式尺寸（JS 动态写入 px），移除 transform translateX（与拖拽冲突），.open 仅控制 opacity/visibility/translateY。
- 新增 8 向 resize handle 样式（.resize-handle.rh-n/s/e/w/ne/nw/se/sw），SE 角带三角抓握提示；hover 微亮。
- 垂直对齐修复：.chat-input padding 10→11px 对称、line-height:20px（=42-22 单行居中）；.chat-composer align-items:center；.chat-close flex 居中 + line-height:1。
- HTML 新增 id="chatDragHandle"（header）+ 8 个 resize-handle span（data-dir）。
- JS DialogController 模块（~120行）：LayoutPersistence（localStorage cyber-chat-layout-v1，独立于对话历史）、DragController（pointerdown/move/up 标题栏拖拽，排除关闭按钮/非左键）、ResizeController（8向缩放，方向映射表含 left/top 联动）、边界约束（MIN 320×240 / MAX vw-16×vh-16 / clamp 到视口内）、移动端降级（isTouchDevice → .static 类回退居中+隐藏手柄）、window.resize 重约束。
- init() 调整顺序：initLayout → bindLayoutEvents → loadHistory → ... → setChat(true)。
- autoGrow 上限 120→160 与 CSS max-height 同步。
- 校验：两段脚本 SYNTAX_OK；13 个 getElementById 引用（含新增 chatDragHandle）全部存在；零残留旧引用。

## 对话框默认位置右移 + 左侧黑客终端数据面板
- 备份当前版本为 `audio-visualizer.backup-20260812-230633.html`（可回退）。
- 对话框默认位置从居中改为右侧（defaultRect: x=vw-w-20, w=min(540,42vw)），距右边缘 20px，宽度收窄适配右侧栏布局。
- 新增左侧 `.term-panel` 黑客终端面板（z-index:4，高于画布低于 HUD）：半透明深色背景(rgba(3,8,14,0.72)) + blur(4px)，极淡绿色边框；移动端 ≤900px 自动隐藏。
- 终端内容为实时滚动的系统监控日志（10 类模板加权随机）：
  - [HEX] 十六进制数据流（权重 30%，最常见）
  - [FFT] 频谱原始数据（权重 20%，读取真实 barLevels/energy）
  - [MEM] 内存分配/释放追踪
  - [SYS] 系统调用日志（read/write/ioctl/mmap 等）
  - [NET] 网络数据包摘要（TCP/UDP/ICMP + flags + seq）
  - [PROC] 进程状态（cpu/mem/threads/pid）
  - [KRN] 内核模块信息
  - [CRYPT] 加密哈希摘要
  - [WRN]/[ERR] 偶发警告（琥珀色高亮）
  - [TSC] 时间戳计数器
- 视觉分级：lvl-0 极暗青色（默认，大部分行）→ lvl-1 中等 → lvl-2 绿色高亮 → lvl-3 琥珀色警告 → lvl-4 红色错误；整体低调不抢焦点。
- 输出节奏 ~8 行/秒（120ms 间隔），最大保留 60 行自动清理旧行；启动时预填充 28 行避免空白。
- `pointer-events: none` 不拦截鼠标交互；`overflow: hidden` 模拟 tail -f 效果。
- 校验：3 段脚本 SYNTAX_OK；14 个 getElementById 引用全部存在（含 termBody/termPanel）；零缺失。

## 回退终端框 + 改做融入画布的黑客数据流特效
- 用户纠正：不要独立"框/面板"，要融入画面的黑客特效。回退了 `.term-panel` 全部 CSS/HTML/JS（终端模块、DOM、animationLoop/init 调用），校验零残留、两段脚本 SYNTAX_OK。
- 新做法：在画布渲染层新增 `drawHackerStream(dt)` —— 左侧限定区域（避开中央环形波形与右侧对话框）绘制流动的黑客代码行（十六进制 dump / 汇编指令 / 系统调用 / 网络端口 / 偶发警告），绿色调半透明（暗 0.16 / 偶尔高亮 0.42），向上漂移、溢出顶部后底部重生，无边框无背景面板，纯背景氛围层。
- 窄屏（viewWidth<640）不绘制保持纯净；文本超宽自动截断避免越界到中央。
- `resizeCanvas` 调用 `initHackerStream()` 初始化行集合；`render()` 在 drawDataRain 之后调用 `drawHackerStream(dt)`。

## 黑客数据流可拖拽 + localStorage 持久化
- 新增 `.hacker-drag-zone` 透明拖拽覆盖层（CSS）：position:fixed, z-index:8, cursor:grab, 极淡绿边框(0.08 opacity)，hover 时微亮(0.25)+左上角三角标记，拖拽中边框加亮(0.40)+阴影；移动端/触屏自动隐藏。
- HTML：`<div class="hacker-drag-zone" id="hackerDragZone">` 放在 `<script>` 标签之前。
- JS 拖拽控制器（~80行）：
  - `hackerPos = {x, y}` 状态变量控制数据流绘制偏移（默认 x=24, y=96）
  - `loadHackerPos()` / `saveHackerPos()` 读写 localStorage（key: `cyber-hacker-pos-v1`），容错处理
  - `syncHackerDragZone()` 同步拖拽区域 DOM 尺寸与位置（跟随 hackerPos + 视口计算区域宽高）
  - pointerdown/move/up 拖拽事件（仅左键、setPointerCapture、边界约束：x∈[-100, vw*0.6], y∈[40, vh-150]）
  - 松手自动 saveHackerPos()；window.resize 时重新约束
- `drawHackerStream()` 的 regionLeft / regionTop 改为读取 `hackerPos.x / hackerPos.y`（不再硬编码 24/96）
- `initHackerStream()` 的 regionTop 同步改为 `hackerPos.y`
- `init()` 中调用 `initHackerDrag()` 初始化（恢复位置+绑定事件+同步DOM）
- 校验：两段脚本 SYNTAX_OK；14 个 getElementById 引用全部存在（含新增 hackerDragZone）；零缺失。

## 组件 ID 标注 + 显隐开关
- 需求：给页面所有动效组件加可显示的参考 ID，方便指代；并增加一个「显示/隐藏组件ID」开关。
- CSS：`.dev-label`（绝对定位、品红描边小标签、pointer-events:none 不挡交互）、`body:not(.dev-mode) .dev-label{display:none}`（默认隐藏）、`.dev-toggle`（底部居中固定按钮，active 态变品红）。
- HTML：17 个动效组件加 `data-dev-id` 属性：fx-scanbar / fx-spectrum(#stage 画布) / fx-title(标题) / hud-tl / hud-tr / hud-bl / hud-br（四角 HUD）/ fx-hacker-stream(黑客流拖拽区) / chat-panel / chat-resize-n/s/e/w/ne/nw/se/sw（8 向缩放手柄）。
- 新增 `#devToggle` 按钮 + 第三段脚本：遍历 `[data-dev-id]` 给每个组件 append 一个 `.dev-label` 子节点（作为子元素自动跟随父组件拖动/缩放）；点击按钮切换 `body.dev-mode` 类并切换按钮文案（显示组件ID ↔ 隐藏组件ID）。
- 校验：3 段脚本 SYNTAX_OK；无缺失 ID；17 个 data-dev-id 全部存在。

## 组件 ID 标注优化（辨识度 + 悬停识别）
- 用户反馈：① ID 看似不唯一（实为视觉重叠导致难分辨）② 标签混在一起分不清哪个是哪个；要求鼠标移到 ID 上时告知当前选中的是哪个。
- CSS：`.dev-label` 改为实心高对比紫色底白字（提升辨识度）；新增 `body.dev-mode [data-dev-id].dev-hover`（悬停青色描边 + 外发光，框出当前组件）；新增 `.dev-readout`（底部居中读数条，仅 dev-mode 显示）。
- HTML：新增 `#devReadout` 读数条元素。
- JS：DEV_NAMES 映射（17 个 ID → 中文名）；`mouseover` 委托（closest data-dev-id）高亮 + 更新读数「当前选中 ▸ <id> · <中文名>」；`mouseout` 离开组件时清除高亮（读数保留最后项方便指代）；setDevMode 同步更新读数默认文案「悬停任意组件 → 查看其参考 ID」。
- 校验：3 段脚本 SYNTAX_OK；17 个 data-dev-id 无重复（确实唯一）；无缺失 ID。

## React 组件化重构（Vite + React 18）
- 在 `c:\Users\Administrator\ai\sound\` 初始化 Vite + React 18 工程，将单文件 `audio-visualizer.html` 拆分为独立可复用组件，**严格保持**原功能/样式/布局/交互/全局契约不变。
- 结构：`src/{main.jsx, App.jsx, styles/cyber.css(原<style>逐字提取), lib/spectrum-engine.js(UMD 修复), data/devNames.js, hooks/{useVizEngine,useChatController,useDevOverlay}.js, components/{background,viz,hud,chat,dev}/...}`。
- 关键决策：① 三个原 IIFE 1:1 搬进自定义 Hook（不改写React状态，避免行为漂移）；② Hook 内部仍按 `id` 查 DOM，组件渲染的 id/class/data-* 与原 HTML 完全一致（已逐项 diff 校验）；③ 引擎 UMD 改为**始终挂载 root.SpectrumEngine**（ESM 下 `module` 未定义，原分支会被跳过，浏览器/测试环境均可用）；④ 不使用 StrictMode（避免 rAF/事件双重初始化）；⑤ CSS 逐字提取未改写。
- 自检（用户要求“完成后自行测试”）：`npm run build` 通过(45 modules, JS 171.78kB/CSS 14.38kB)；`node scripts/verify-engine.cjs` 引擎逻辑 10/10 断言通过；`vite preview` HTTP 200；全局契约/ DOM id 契约/ data-dev-id·data-form 契约/ 37 个类名覆盖 均一致。
- 局限：本环境无浏览器二进制，无法做交互级冒烟测试；以字节级忠实移植 + 引擎单测 + 静态契约校验作为功能保障。报告见 `VERIFICATION.md`；复现命令 `npm test` / `npm run verify`。
- 注意：构建时若 `dist` 已存在，Vite 的 emptyDir 会触发沙箱 safe-delete 拦截导致 build 失败；需先 `rm -rf dist` 再 `npm run build`（dist 为可重建产物）。
