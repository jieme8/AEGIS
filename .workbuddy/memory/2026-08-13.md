# 2026-08-13

- 修复组件编辑 ID 视觉重复问题：根源是对话框 8 个缩放手柄（chat-resize-*）各自带长标签，全部挤在对话框四角/四边，叠加 chat-panel 标签形成「一堆 chat-resize-/chat-panel」的视觉堆叠，被误认为重复 ID。
  - 处理：移除 8 个缩放手柄的 data-dev-id（它们是子控件，非独立可指代组件，保留 data-dir 缩放功能不受影响）。
  - 结果：仅剩 9 个真正独立的组件 ID（fx-scanbar / fx-spectrum / fx-title / hud-tl / hud-tr / hud-bl / hud-br / fx-hacker-stream / chat-panel），全部唯一、不堆叠。
  - 保留：dev-mode 显隐开关 + 悬停青色描边 + 底部读数条（ID+中文名）。

- 增强编辑 ID 功能（按用户两点要求）：
  1. 方便复制：所有画布标签点击即复制 ID（navigator.clipboard + execCommand 兜底），附「已复制 ✓」青色提示与标签闪绿反馈。
  2. 不可移动组件/背景特效也能悬停与复制：
     - 根因：.hud / .title-wrap / 背景层（grid/vignette/scanlines）原本 pointer-events:none，鼠标穿透到画布只能高亮 fx-spectrum，故「移上去没效果」。
     - 编辑模式下给 .hud / .title-wrap 开启 pointer-events:auto，使其可被悬停选中。
     - 新增中央环形波形热区 .dev-zone-ring（编辑模式可悬停选中 fx-ring）。
     - 新增「组件清单」可折叠面板（dev-legend），列出全部 15 个组件（含 fx-grid/fx-data-rain/fx-scanlines/fx-vignette/fx-glitch 等背景特效），逐行悬停识别 + 点击复制；背景特效层用 data-dev-nolabel 仅进清单、不出画布标签，避免顶部堆叠。
  3. 校验：3 段脚本语法 OK；13 个真实 HTML data-dev-id 全唯一；39 个 id 定义、0 缺失引用。

- 修复 4 个组件 ID 编辑问题（用户反馈）：
  1. fx-hacker-stream 点击无法复制编辑 ID → onHackerDragStart 增加 e.target.closest(".dev-label") 判断，命中时跳过拖拽让 copy 正常触发。
  2. fx-hacker-stream 应仅在 dev-mode 下可拖拽/选中 → CSS 改为默认 pointer-events:none，仅 body.dev-mode 下 auto。
  3. 重置 hacker-stream 到初始化默认位置 → loadHackerPos() 强制返回 null + 清除 localStorage key cyber-hacker-pos-v1。
  4. 频谱柱区域没有组件 ID（截图所示底部霓虹柱阵列）→ 新增 .dev-zone-bars 热区 data-dev-id="fx-bars"（覆盖底部频谱区域），加入 DEV_NAMES 和组件清单。现共 15 个唯一组件 ID。

- 视觉与交互增强（用户两点要求）：
  1. fx-bars / fx-ring 低调化：柱体 shadowBlur 14→6、渐变亮度 70/42%→58/38%、顶部高光与峰值线 alpha 0.9→0.5；环形波形振幅系数 0.28+0.5e→0.16+0.32e、shadowBlur 16→8、lineWidth 3→2、光晕 0.12→0.06、能量核幅度收敛。仅降视觉强度，动态保留。
  2. AI 回复联动动态特效：viz 脚本新增 window.CyberFx（thinking/output/idle）+ fxState + drawFxOverlay()。思考中→中央旋转扫描弧；输出内容→扩散双涟漪+轻微闪屏+频谱柱脉冲(0.9s)后回落 idle。chat 脚本在 showTyping 调 thinking()，appendMessage 后调 output() 并 setTimeout(900ms) idle()，异常时立即 idle()。OUTPUT_BURST_MS=900 与 viz OUTPUT_BURST=0.9s 对齐。

- 用户纠正两点（重要设计偏好）：
  1. fx-ring/fx-bars 只要“现有动画低调化 + 随 chat 交互反应”，不要新增覆盖层动画。→ 删除 drawFxOverlay（扫描弧/涟漪/闪屏），改为在 render 内用语义倍率 fxMod 调制现有柱高与环振幅：思考中 sin 呼吸收敛(0.80–0.90)、输出时脉冲放大(+55%,0.9s)。
  2. 编辑模式下 fx-hacker-stream 与 chat-panel 显示实时坐标。→ 标签用 dataset.copyId 存纯 ID（复制不受影响），新增 updateCoordLabels + 120ms 定时器（拖拽/缩放实时跟随），退出编辑模式停定时器并还原纯 ID。格式：hacker-stream 显 "x,y"，chat-panel 显 "x,y w×h"。
  - 教训：用户要的是“驱动现有元素”，不是“叠加新特效层”。

- 新增可视化形态切换（用户明确要求）：
  1. fx-title 内容改为「J.A.R.V.I.S.」+ 副标题「Just A Rather Very Intelligent System」（glitch data-text 同步）。
  2. fx-ring / fx-bars 增加 3 种可切换形态：default（原低调频谱/环波）、thinking（柱=平滑流动正弦波青色调+环=平滑圆呼吸+旋转扫描弧）、output（柱=高能喷涌 traveling spike 放大+环=快速旋转+扩散同心涟漪）。
  3. 顶部新增 .form-switch 按钮组（默认/思考/输出），bindFormButtons 绑定 window.CyberFx.setForm；chat 交互复用 window.CyberFx.thinking()/output()/idle() 自动切到对应形态（空闲回 default）。vizForm 状态 + syncFormButtons 同步高亮。
  4. 重构：render 改为调用 drawSpectrumBars(vizForm)/drawRingWave(vizForm)；移除 fxMod/fxState.mode/burstT/OUTPUT_BURST（旧 chat 反应机制被形态机制取代）。注意 OUTPUT_BURST_MS 是 chat 脚本本地常量，与 viz 无关。

- 小修：fx-form-switch 按钮组加 data-dev-id（"形态切换按钮组"），默认隐藏仅 dev-mode 可见；chat 自动联动确认：thinking→思考形态、output→输出形态、idle→默认形态（900ms 后回落）。

- 优化动画与过渡（用户要求）：
  1. 默认形态增强表现力/辨识度：柱体 glow 6→13、渐变 58/38→68/48%、顶高光 0.5→0.8、峰值线 0.5→0.75、柱高×1.04；环波振幅 0.16+0.32e→0.19+0.38e、glow 8→15、线宽 2→2.6、亮度 56→64%、饱和 90→95。三者（默认霓虹/思考青正弦/输出品红喷涌）视觉区分明显。
  2. 形态切换过渡：先做成 0.55s easeInOutCubic 交叉溶解（淡出+淡入），FORM_DUR=0.55s。
  3. 用户反馈：交叉淡出不够，要“动画形变”过渡。重构为逐柱/逐段参数插值（新增 barStyle/ringStyle/ringRadius 辅助函数，描述单形态可插值参数），FORM_DUR=0.65s：
     - 频谱柱：高度/发光/顶部高光/色相(hsl)逐柱线性插值 → 物理变形（非淡出）。
     - 环形：半径在“圆(思考)”↔“波形(默认/输出)”逐段插值、旋转速度 rot 插值、线宽/发光/亮度/饱和/色相插值 → 圆与波形互相变形。
     - 签名特效融入形变：扫描弧/涟漪/能量核按“目标形态占比”e（或起始形态 1-e）淡入淡出，不再硬切/双绘。
     - render 不再分稳态/过渡分支，始终调用 drawSpectrumBars(from,to,e)/drawRingWave(from,to,e) 插值绘制，e==easeInOutCubic(formMix)。

- 接入真实 AI 聊天（LongCat，用户提供的 key）：
  - 新增 `src/config/modelConfig.js`：endpoint `https://api.longcat.chat/openai/v1/chat/completions`、Bearer `ak_2jJ7rL9fb9re8xg22J4Vu26H6RY6k`、model `LongCat-2.0`、maxTokens 1000、timeout 30s、fallbackToLocal true。
  - 改造 `useChatController.getAIResponse`：构造 system 消息（含实时频谱快照 `window.SpectrumAPI.snapshot()` 文本化）+ 最近 12 轮历史，调用 `callLongCat()`（fetch + AbortController 超时）。
  - 失败时（网络/密钥/限流/空 content）自动回退 `window.SpectrumEngine.buildSpectrumReply(snap,text)`，对话不中断。
  - 验证：curl 实测 HTTP 200、content 正常返回（推理模型，需 maxTokens≥1000 才有正文）；CORS 预检放行（Access-Control-Allow-Origin 按源反射、允许 authorization/content-type），纯前端浏览器可直接调用，无需后端代理。
  - 安全提示：密钥进前端 bundle，仅适合本地/演示；生产应走自有后端代理 + `import.meta.env.VITE_LONGCAT_API_KEY`。

- 坐标显示功能扩展到【所有】组件（用户要求“为所有组件实现坐标显示”）：
  - 原逻辑仅 fx-hacker-stream / chat-panel 显坐标（COORD_IDS 白名单）。改为移除白名单，所有带画布标签的组件（fx-title/fx-spectrum/fx-hacker-stream/fx-form-switch/fx-ring/fx-bars/chat-panel/hud-4角，共 11 个）均实时显示 X/Y；chat-panel 额外显 W×H（可缩放）。
  - 标签结构化为两段 `.dl-id`（ID，粗白）+ `.dl-coord`（坐标，青色，竖线分隔），inline-flex 基线对齐，格式 `X:123 Y:45`（聊天 `X:123 Y:45 300×400`）。
  - 复制仍用 dataset.copyId（纯 ID 不受坐标影响）；退出 dev-mode 清空 `.dl-coord`。
  - 同步修改 React 源 `src/hooks/useDevOverlay.js` + `src/styles/cyber.css` 与 standalone `audio-visualizer.html`（内联脚本 + `<style>`），保持双版本一致。
  - 验证：`npm run verify`（引擎10/10、CSS 无缺失类）通过，`npm run build` 成功（dist 已重建）。注意：本环境无回收站，vite 清空旧 dist 时 safe-delete 失败需 `mv dist dist_bak` 规避后再 build。
  - 注：背景全屏层（fx-grid/fx-vignette/fx-scanlines/fx-scanbar）带 data-dev-nolabel，按设计不出画布标签故不显坐标。

- 新增【油价显示组件】OilPricePanel（与音频可视化同套赛博视觉语言）：
  - 文件：`src/components/data/OilPricePanel.jsx`（组件）+ `src/components/data/oil-price.css`（自包含样式）。
  - 特性：数据驱动 props、主图大价+数字计数动效(useCountUp easeOutCubic)、涨跌幅（中国习惯 涨=红#ff5470 跌=绿#2fe6a0）、SVG 迷你趋势线、当日区间进度条、多品种卡片网格。
  - 预览入口：`oil-price-demo.html` + `src/oil-price-demo/main.jsx`（mock 数据 + 2s 实时模拟刷新）。
  - 运行：`npm run dev` → 若 5173 被占则 5174，访问 `/oil-price-demo.html`。

- 模型对话过程浮层改为【嵌入式局部浮层 + 可拖动】（用户要求：不要全屏弹窗，要嵌入子框、不阻断页面交互、可拖动）：
  - `ChatTraceDrawer.jsx`：移除全屏 `.trace-backdrop` 遮罩；改为锚定父容器 `.chat-panel` 内的 `.trace-float`（`position:absolute`），`aria-modal="false"`；标题栏 `onPointerDown` 指针拖动（`offsetParent` 取 chat-panel 为边界，拖动时 clamp 在父容器内）。
  - `ChatPanel.jsx`：将 `<ChatTraceDrawer>` 从 aside 的兄弟节点移入 `<aside className="chat-panel">` 内部，使其随聊天面板一起移动/缩放。
  - `cyber.css`：`.trace-float` 替换原 `.trace-backdrop`/`.trace-drawer`（`top:52px;left:14px;width:420px;max-width:calc(100%-28px);max-height:calc(100%-66px);z-index:5`）；`.trace-head` 加 `cursor:grab/touch-action:none/user-select:none` 作拖拽手柄；`.trace-body` 加 `flex:1 1 auto;min-height:0` 以在浮层内独立滚动。
  - 验证：`npm run build` 通过（48 modules）；dev server 已 HMR 提供最新代码。本环境无浏览器，未做交互级拖动实测（逻辑用 offsetParent 边界 clamp，标准实现）。

- 修正：浮层改为【完全独立、整页任意拖动】，而非聊天面板内嵌入（用户纠正："可在整个页面随便拖动，独立存在，不受任何限制"）：
  - 原因：`.chat-panel` 有 `backdrop-filter: blur(9px)`，会使其成为 `position:fixed` 后代的包含块，故必须把浮层挂到 body 才能脱离面板、相对视口浮动。
  - `ChatTraceDrawer.jsx`：用 `createPortal(<.../>, document.body)` 渲染；`position: fixed`，默认锚点视口右上（`top:70px;right:20px`）；拖动时 `box.style.right='auto'` 改用 left 驱动；边界 clamp 改为 `window.innerWidth/innerHeight`（仅防止拖出屏幕丢失，不再限制于任何父容器）。
  - `ChatPanel.jsx`：浮层从 aside 内部移回兄弟节点（Fragment），由 Portal 落到 body。
  - `cyber.css`：`.trace-float` 改 `position:fixed`、视口级 `max-width:calc(100vw-40px)`、`max-height:86vh`、`z-index:60`（高于聊天面板）。
  - 验证：`npm run build` 通过；dev server HMR 命中 Portal/body/innerWidth/fixed。

- 浮层改为【紧凑设计】+ 补【组件ID】（用户要求"改成紧凑的设计"且"没有组件ID"）：
  - 紧凑：`.trace-float` 宽 420→340px、圆角12→10、阴影略减；头/体/分区/代码/回复等全部缩小内边距与字号（头 padding 12→7、标题13→12、status 11→10、body gap10→7、summary 10→7、code 11.5→10.5/最大高220→150、reply 13→12/行高1.7→1.6、cursor 8×15→7×13、loading tdot 6→5 等）。
  - 组件ID：根 div 加 `data-dev-id="trace-float"`；因浮层是动态挂载（dev 覆盖层仅在 mount 时给静态元素打标签，会漏掉它），改为在 React 内自行渲染 `.dev-label` 末子节点（含 `.dl-id`/`.dl-coord`，`data-copy-id`），重渲染安全且 dev 坐标定时器会自动填充；加 `copyDevId()` 点击复制并复用 `#devToast`。`src/data/devNames.js` 增 `"trace-float":"模型对话过程浮层"` 并加入"主组件/交互"分组，使其出现在组件清单。
  - 验证：`npm run build` 通过；dev server 命中 data-dev-id/dev-label/trace-float/width:340px。

- OilPricePanel 重构为【紧凑单品种】版（用户要求：标签太大→缩小、只显示92#汽油、不需要一直动、加调价倒计时+预估）：
  - 去掉持续 2s 模拟 tick（不再跳动，仅加载时一次计数动效）；去掉对零售价无意义的当日区间/趋势线(SVG)；新增 props nextAdjust(倒计时)、forecast{direction,text}(预估上调/下降)、basis(涨跌基准说明)、title/subtitle。
  - 用户定位为「上海 92# 汽油」零售指导价（非原油期货）；零售价由发改委每~10工作日调一次、平时固定 → 天然不该实时抖动。
  - 真实数据(2026-08-13查证)：上海92#=¥7.93/升(08-01调价+0.55，调价前7.38)；下次调价 2026-08-14 24:00(还剩1天)；机构(金联创/卓创)预估新一轮小幅下调概率较大。demo 已硬编码真实值、去掉 mock tick。
