# 2026-08-13

- 修复组件编辑 ID 视觉重复问题：根源是对话框 8 个缩放手柄（chat-resize-*）各自带长标签，全部挤在对话框四角/四边，叠加 chat-panel 标签形成「一堆 chat-resize-/chat-panel」的视觉堆叠，被误认为重复 ID。
  - 处理：移除 8 个缩放手柄的 data-dev-id（它们是子控件，非独立可指代组件，保留 data-dir 缩放功能不受影响）。
  - 结果：仅剩 9 个真正独立的组件 ID（fx-scanbar / fx-spectrum / fx-title / hud-tl / hud-tr / hud-bl / hud-br / fx-hacker-stream / chat-panel），全部唯一、不堆叠。
  - 保留：dev-mode 显隐开关 + 悬停青色描边 + 底部读数条（ID+中文名）。

- 增强编辑 ID 功能（按用户两点要求）：
  1. 方便复制：所有画布标签点击即复制 ID（navigator.clipboard + execCommand 兜底），附「已复制 ✓」青色提示与标签闪绿反馈。
  2. 不可移动组件/背景特效也能悬停与复制：
     - 根因：.hud / .title-wrap / 背景层（grid/vignette/scanlines）原本 pointer-events:none，鼠标穿透到画布只能高亮 fx-spectrum，故「移上去没效果」。
     - 编辑模式下给 .hud / .title-wrap 开启 pointer-events:auto，使其可被悬停选中。
     - 新增中央环形波形热区 .dev-zone-ring（编辑模式可悬停选中 fx-ring）。
     - 新增「组件清单」可折叠面板（dev-legend），列出全部 15 个组件（含 fx-grid/fx-data-rain/fx-scanlines/fx-vignette/fx-glitch 等背景特效），逐行悬停识别 + 点击复制；背景特效层用 data-dev-nolabel 仅进清单、不出画布标签，避免顶部堆叠。
  3. 校验：3 段脚本语法 OK；13 个真实 HTML data-dev-id 全唯一；39 个 id 定义、0 缺失引用。

- 修复 4 个组件 ID 编辑问题（用户反馈）：
  1. fx-hacker-stream 点击无法复制编辑 ID → onHackerDragStart 增加 e.target.closest(".dev-label") 判断，命中时跳过拖拽让 copy 正常触发。
  2. fx-hacker-stream 应仅在 dev-mode 下可拖拽/选中 → CSS 改为默认 pointer-events:none，仅 body.dev-mode 下 auto。
  3. 重置 hacker-stream 到初始化默认位置 → loadHackerPos() 强制返回 null + 清除 localStorage key cyber-hacker-pos-v1。
  4. 频谱柱区域没有组件 ID（截图所示底部霓虹柱阵列）→ 新增 .dev-zone-bars 热区 data-dev-id="fx-bars"（覆盖底部频谱区域），加入 DEV_NAMES 和组件清单。现共 15 个唯一组件 ID。

- 视觉与交互增强（用户两点要求）：
  1. fx-bars / fx-ring 低调化：柱体 shadowBlur 14→6、渐变亮度 70/42%→58/38%、顶部高光与峰值线 alpha 0.9→0.5；环形波形振幅系数 0.28+0.5e→0.16+0.32e、shadowBlur 16→8、lineWidth 3→2、光晕 0.12→0.06、能量核幅度收敛。仅降视觉强度，动态保留。
  2. AI 回复联动动态特效：viz 脚本新增 window.CyberFx（thinking/output/idle）+ fxState + drawFxOverlay()。思考中→中央旋转扫描弧；输出内容→扩散双涟漪+轻微闪屏+频谱柱脉冲(0.9s)后回落 idle。chat 脚本在 showTyping 调 thinking()，appendMessage 后调 output() 并 setTimeout(900ms) idle()，异常时立即 idle()。OUTPUT_BURST_MS=900 与 viz OUTPUT_BURST=0.9s 对齐。

- 用户纠正两点（重要设计偏好）：
  1. fx-ring/fx-bars 只要“现有动画低调化 + 随 chat 交互反应”，不要新增覆盖层动画。→ 删除 drawFxOverlay（扫描弧/涟漪/闪屏），改为在 render 内用语义倍率 fxMod 调制现有柱高与环振幅：思考中 sin 呼吸收敛(0.80–0.90)、输出时脉冲放大(+55%,0.9s)。
  2. 编辑模式下 fx-hacker-stream 与 chat-panel 显示实时坐标。→ 标签用 dataset.copyId 存纯 ID（复制不受影响），新增 updateCoordLabels + 120ms 定时器（拖拽/缩放实时跟随），退出编辑模式停定时器并还原纯 ID。格式：hacker-stream 显 "x,y"，chat-panel 显 "x,y w×h"。
  - 教训：用户要的是“驱动现有元素”，不是“叠加新特效层”。

- 新增可视化形态切换（用户明确要求）：
  1. fx-title 内容改为「J.A.R.V.I.S.」+ 副标题「Just A Rather Very Intelligent System」（glitch data-text 同步）。
  2. fx-ring / fx-bars 增加 3 种可切换形态：default（原低调频谱/环波）、thinking（柱=平滑流动正弦波青色调+环=平滑圆呼吸+旋转扫描弧）、output（柱=高能喷涌 traveling spike 放大+环=快速旋转+扩散同心涟漪）。
  3. 顶部新增 .form-switch 按钮组（默认/思考/输出），bindFormButtons 绑定 window.CyberFx.setForm；chat 交互复用 window.CyberFx.thinking()/output()/idle() 自动切到对应形态（空闲回 default）。vizForm 状态 + syncFormButtons 同步高亮。
  4. 重构：render 改为调用 drawSpectrumBars(vizForm)/drawRingWave(vizForm)；移除 fxMod/fxState.mode/burstT/OUTPUT_BURST（旧 chat 反应机制被形态机制取代）。注意 OUTPUT_BURST_MS 是 chat 脚本本地常量，与 viz 无关。

- 小修：fx-form-switch 按钮组加 data-dev-id（"形态切换按钮组"），默认隐藏仅 dev-mode 可见；chat 自动联动确认：thinking→思考形态、output→输出形态、idle→默认形态（900ms 后回落）。

- 优化动画与过渡（用户要求）：
  1. 默认形态增强表现力/辨识度：柱体 glow 6→13、渐变 58/38→68/48%、顶高光 0.5→0.8、峰值线 0.5→0.75、柱高×1.04；环波振幅 0.16+0.32e→0.19+0.38e、glow 8→15、线宽 2→2.6、亮度 56→64%、饱和 90→95。三者（默认霓虹/思考青正弦/输出品红喷涌）视觉区分明显。
  2. 形态切换过渡：先做成 0.55s easeInOutCubic 交叉溶解（淡出+淡入），FORM_DUR=0.55s。
  3. 用户反馈：交叉淡出不够，要“动画形变”过渡。重构为逐柱/逐段参数插值（新增 barStyle/ringStyle/ringRadius 辅助函数，描述单形态可插值参数），FORM_DUR=0.65s：
     - 频谱柱：高度/发光/顶部高光/色相(hsl)逐柱线性插值 → 物理变形（非淡出）。
     - 环形：半径在“圆(思考)”↔“波形(默认/输出)”逐段插值、旋转速度 rot 插值、线宽/发光/亮度/饱和/色相插值 → 圆与波形互相变形。
     - 签名特效融入形变：扫描弧/涟漪/能量核按“目标形态占比”e（或起始形态 1-e）淡入淡出，不再硬切/双绘。
     - render 不再分稳态/过渡分支，始终调用 drawSpectrumBars(from,to,e)/drawRingWave(from,to,e) 插值绘制，e==easeInOutCubic(formMix)。
